

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# WordPress MCP — credenciais reais (não commitar)
tools/wordpress-mcp/wp-sites.json

# MCP Config — contém tokens sensíveis
.mcp.json

# Google/GTM Credentials
tools/gtm-mcp/credentials/

# Meta Ads — contas com tokens reais
tools/meta-ads-mcp/accounts.json

# Next.js build (squads)
squads/**/.next/
**/.next/
